# ============================================================
# conda 环境定义（锁 Python 版本，可复现）
# 用法：conda env create -f environment.yml
# ============================================================
name: customer-agent
channels:
  - conda-forge
dependencies:
  - python=3.12            # 锁 3.12（3.14 太新，LangGraph 生态可能未适配）
  - pip
  - pip:
      - -r requirements.txt
